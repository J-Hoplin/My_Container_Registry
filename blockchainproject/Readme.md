## Block Chain Project Server
***
### Execution Command

- Image Base Spec : [ubuntu latest](https://hub.docker.com/_/ubuntu)

```
docker build -t (image_name) .

docker run -it -d --name (container_name) (image_name) bash 

docker exec -it (container_name) bash
```

